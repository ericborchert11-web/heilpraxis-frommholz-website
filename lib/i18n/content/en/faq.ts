import type { FaqItem } from '@/lib/faq-global';

/** Noch nicht übersetzt — `undefined` heißt: auf `/en` erscheint die deutsche FAQ. */
export const GLOBAL_FAQ_EN: readonly FaqItem[] | undefined = undefined;
