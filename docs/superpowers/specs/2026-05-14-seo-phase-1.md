# SEO Phase 1 — Implementation Plan

**Datum:** 2026-05-14
**Quelle:** `briefings/SEO-MEGA-BRIEFING.md` (Welle 1)
**Status:** in Umsetzung

---

## Scope (was in dieser Session passiert)

Welle 1 des Briefings, exakt nach dessen eigenem Iterationsplan:

1. **TEIL 1 — Technisches SEO-Fundament**
   - `lib/site-config.ts` ergänzen um `legalName`, `defaultOgImage`, `geo`-Koordinaten, Service-Areas
   - `app/sitemap.ts` (statisch generierte Sitemap mit Pflichtseiten + Leistungen-Hub + Money-Pages)
   - `app/robots.ts`
   - `components/SEO/JsonLd.tsx` (`MedicalBusinessJsonLd`, `ServiceJsonLd`)
   - `components/SEO/FaqJsonLd.tsx`
   - `components/SEO/BreadcrumbJsonLd.tsx`
   - `components/Breadcrumbs.tsx` (sichtbar)
   - `app/layout.tsx` — MedicalBusiness-JSON-LD im `<head>`

2. **TEIL 2 — Rechtspflichtseiten**
   - `app/impressum/page.tsx`
   - `app/datenschutz/page.tsx`
   - `app/agb/page.tsx`

3. **TEIL 3 (1–3) — Money-Pages mit Inhalten**
   - `app/leistungen/page.tsx` (Hub-Übersicht aller 8 Briefing-Leistungen)
   - `app/leistungen/[slug]/page.tsx` (dynamic + `generateStaticParams`)
   - Inhalt für `sitzwachen-berlin`, `systemsprenger-betreuung`, `pflegedienst-selbstzahler`
   - Andere 5 Leistungen (24h, Pflegeberatung, Nachtwachen, Hospiz, Reisebegleitung) bekommen in Welle 2 ihre Inhalte; sind in `lib/leistungen-seo.ts` als Stub angelegt.

4. **TEIL 7.1 — Globale FAQ auf Startseite**
   - `lib/faq-global.ts` mit 8 FAQs
   - `components/FaqAccordion.tsx`
   - FAQ-Sektion in `app/page.tsx` einsetzen, mit `FaqJsonLd`

5. **Footer-Fix**
   - Leistungen-Spalte zeigt die 8 Briefing-Slugs (3 davon haben Detail-Seite, 5 sind noch ohne)

## Explizit out of scope (Welle 2/3)

- TEIL 4 (8 Themen-Hub-Seiten)
- TEIL 5 (11 Stadtteil-Seiten + Standorte-Hub mit Karte)
- TEIL 6 (5 Recruiting-Seiten + Karriere-Hub)
- Startseiten-Hero-Umbau (TEIL 7.2)
- Money-Pages 4–8

## Konflikt-Auflösung (siehe Memory `seo-briefing-vs-repo-conflicts`)

| Briefing-Wortlaut | Tatsächliche Umsetzung |
|---|---|
| Telefon `030 6446 1243`, Fax `030 6446 1245`, Mail `kontakt@…` | Handy `0151 14140251`, kein Fax, `simeon@heilpraxis-frommholz.de` (per Memory + bestehendem Code) |
| Daten in `content/*.ts` | Daten in `lib/*.ts` (Repo-Konvention) |
| `app/leistungen/{slug}/page.tsx` statische Ordner | `app/leistungen/[slug]/page.tsx` dynamic mit `generateStaticParams` |

## Reihenfolge der Edits

Tasks #1–9 abarbeiten in dieser Reihenfolge:
1. **Plan** (diese Datei) ✓
2. **Daten**: site-config, leistungen-seo, faq-global
3. **Sitemap & Robots**
4. **SEO-Komponenten**
5. **Layout**: MedicalBusiness-JSON-LD global
6. **Rechtsseiten** (Impressum, Datenschutz, AGB)
7. **Leistungen** (Hub + dynamic detail + 3 ausgefüllte Inhalte)
8. **FAQ + Footer**
9. **Verifikation** (lint, typecheck, build)

## Offene Punkte (an Eric, sobald er reagiert)

Nicht-blockierend für Welle 1; an konkreten Stellen Platzhalter:

- Berufshaftpflicht-Versicherer (Impressum): `[wird nachgereicht]`
- OG-Default-Image (`/og-default.jpg`): Datei nachzulegen
- Bildnachweise (Impressum): leer
- Steuernummer: privat, nicht öffentlich angezeigt
